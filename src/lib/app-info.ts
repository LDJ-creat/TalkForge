export const APP_NAME = "TalkForge";
export const APP_TAGLINE = "AI English speaking practice — P0 app shell";

export function getAppShellDescription(): string {
  return `${APP_NAME} is the P0 foundation for realtime voice role-play and async teaching feedback.`;
}
